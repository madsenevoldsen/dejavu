package com.jayway.dejavu.core.value;

/**
 * A marker interface for values, i.e. immutable values.
 *
 * Use cases only accept values as input and
 * Providers only outputs values
 */
public interface Value {
}
